import sys
test_cases = open(sys.argv[1], 'r')
for test in test_cases:
    test = test.strip().split(' ')
    test.sort(reverse = True)
    for mem in test:
        print mem,
    print
test_cases.close()
